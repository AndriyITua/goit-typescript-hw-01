// type User = {
//   name: string;
//   age: number;
// };

// let person: User[] = [{ name: "Max", age: 21 }];

// console.log(person);

let person: [string, number];
person = ["Max", 21];
console.log(person);
